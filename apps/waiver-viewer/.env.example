VITE_API_BASE_URL=http://localhost:3001

# Optional convenience for private/trusted deployments only.
# This must match the API service's ADMIN_API_KEY.
# Because this is a Vite frontend variable, it is visible in the browser bundle.
VITE_ADMIN_API_KEY=
